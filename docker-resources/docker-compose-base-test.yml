services:
  db:
    image: postgres:15-alpine
    ports:
      - "54350:5432"
    environment:
      POSTGRES_DB: object-management-test
      POSTGRES_USER: test-user
      POSTGRES_PASSWORD: password
